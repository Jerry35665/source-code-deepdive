# 全系列总目录:逐篇索引

> 《源码深读 · 系统开源项目解读系列》137 篇正文的逐篇一行索引(不含各 series 的 00 导读与卷末总结类目已标注)。行号基线见各系列 00-导读。

## 第一系列:系统软件主线(65 篇)

### 卷一 Redis(9 篇+6 报告)
01-redis:01 全景与代码地图 / 02 从 main 开始-服务器生命周期与命令执行 / 03 数据结构底座 SDS·dict·listpack / 04 五大类型与底层编码 / 05 事件循环与网络 IO(io_threads 两代) / 06 持久化 RDB 与 Multi-Part AOF / 07 复制·哨兵与集群(replid 时钟/16384 推导) / 08 内存·过期·淘汰与工程收尾(ebuckets)

### 卷二 SQLite(10 篇+6 报告)
02-sqlite:01 全景与代码地图 / 02 执行框架-从 open 到 step / 03 页面层与 B 树 / 04 事务·锁与 WAL / 05 记录格式与类型系统 / 06 编译前端-词法·lemon·表达式 / 07 查询编译与优化器 / 08 VDBE 虚拟机 / 09 工程文化与测试体系

### 卷三 LevelDB(10 篇+5 报告)
03-leveldb:01 全景与代码地图 / 02 执行框架-从 Open 到 Get 与 Put / 03 内存组件-跳表与 Arena / 04 SSTable 磁盘格式 / 05 BloomFilter 与读优化 / 06 版本管理与 LSM 分层 / 07 Compaction 与迭代器 / 08 WAL 与崩溃恢复 / 09 工程文化与接口设计

### 卷四 etcd/raft(7 篇+4 报告)
04-etcd:01 全景与代码地图 / 02 Raft 核心状态机 / 03 日志复制与进度 / 04 选举安全与 ReadIndex / 05 Node 协议与 etcd 集成 / 06 卷末总结

### 卷五 Nginx(8 篇+5 报告)
05-nginx:01 全景与代码地图 / 02 启动与进程模型 / 03 事件循环与连接 / 04 HTTP 框架与请求生命周期 / 05 Upstream 与反向代理 / 06 内存池与数据结构 / 07 模块系统与工程文化

### 卷六 Kafka(7 篇+4 报告)
06-kafka:01 全景与代码地图 / 02 生产者 / 03 消费者与协调 / 04 日志存储与副本 / 05 网络层与 KRaft / 06 卷末总结

### 卷七 Kubernetes(7 篇+4 报告)
07-k8s:01 全景与代码地图 / 02 API 服务器 / 03 controller 机制 / 04 调度器 / 05 kubelet / 06 卷末总结

### 卷八 llama.cpp(7 篇+4 报告)
08-llamacpp:01 全景与代码地图 / 02 推理循环与计算图 / 03 模型加载与 KV 缓存 / 04 量化体系与 ggml / 05 采样与服务 / 06 卷末总结

## 第二系列:FFmpeg 深读(31 篇,基线 9f63b36a)

| 章 | 主题 | 章 | 主题 |
|---|---|---|---|
| 01 | 全景与代码地图 | 16 | CLI 转码调度(五类线程+两层背压) |
| 02 | avio 与 demuxer(MP4 案例) | 17 | RTSP 与 RTP |
| 03 | codec 框架(send/receive) | 18 | avutil 工具箱(AVOption 伪反射) |
| 04 | filter 框架(activate) | 19 | 视频滤镜族 |
| 05 | mux/硬件/工程文化 | 20 | 系列终章(十大模式) |
| 06 | 首卷总结 | 21 | ffplay 播放器 |
| 07 | 音频管线(swresample) | 22 | parser 层 |
| 08 | 视频编解码器(H.264) | 23 | AV1 生态 |
| 09 | 网络流协议(RTMP/HLS/DASH) | 24 | BSF 家族 |
| 10 | 封装格式对比(MKV/TS/MOV) | 25 | 音频滤镜与编码器(AAC) |
| 11 | 字幕与附件 | 26 | 硬件后端补全(QSV/MediaCodec/D3D12VA/AMF) |
| 12 | 硬件加速后端(VAAPI/NVDEC/VT) | 27 | 加密栈(crypto→CENC 四层) |
| 13 | seek 架构(四层决策树) | 28 | 图片管线(image2/PNG/JPEG/GIF) |
| 14 | x264/x265 封装 | 29 | mux 对比(增量/回填/广播) |
| 15 | swscale(描述符链流水线) | 30 | Vulkan 计算 |

## 第三系列:Git 深读(12 篇,基线 47ce805)

01 对象数据库 / 02 引用与 HEAD(files+reftable 双后端) / 03 索引与状态机(racy git) / 04 packfile 与 delta / 05 传输协议(protocol v2) / 06 合并与变基(merge-ort) / 07 工程文化 / 08 性能基建(commit-graph/bitmap) / 09 fsck 与抢救 / 10 submodule 与 worktree / 11 builtin 架构

## 第四系列:PostgreSQL 深读(15 篇,基线 8c7a74c)

01 进程架构(崩溃级联) / 02 缓冲管理(clock sweep) / 03 WAL 与恢复(FPW/组提交) / 04 SLRU 与检查点(三步锚定) / 05 锁体系(三层金字塔) / 06 MVCC 与快照(可见性矩阵) / 07 VACUUM 与冻结(三重债) / 08 执行器(volcano+EEOP) / 09 B-tree 索引(蟹行/分裂) / 10 复制与逻辑解码(槽/snapbuild) / 11 规划器(支配淘汰/correlation²) / 12 分区(三策略+三次剪枝) / 13 TOAST(四形态分片) / 14 统计与 ANALYZE(两阶段采样)

## 第五系列(2026-09-13 夜间批次)

### zstd 深读(7 篇,基线 d79e723)
01 全景与帧格式(FHD 逐位/块三类型) / 02 熵编码(FSE tANS+Huffman 4 流) / 03 压缩前端(策略族/repcode/最优解析) / 04 序列与块写出(两遍管道) / 05 解码器(wildcopy 契约) / 06 字典与工程(decodecorpus 反证法)

### QuickJS 深读(7 篇,基线 04be246)
01 全景(6 万行地图/NaN-boxing 仅 32 位/JSShape) / 02 词法与解析器(直出字节码无 AST) / 03 字节码与解释器(四段帧/computed-goto/栈扫描异常) / 04 对象模型与 GC(shape 共享/三遍清环) / 05 正则与 Unicode(REOP 回溯/undo 日志) / 06 内置库与工程(宿主义务/libbf 移除勘误)

### curl 深读(5 篇,基线 0b04700)
01 全景与 Multi(easy 是 multi 的糖/MSTATE/cpool) / 02 URL 解析与认证(认证轮/libpsl) / 03 HTTP 栈(cfilter 洋葱/H1 交换格式) / 04 TLS 与连接管理(双层竞速/后端矩阵)

### Caddy 深读(5 篇,基线 56e3a88)
01 全景与模块(配置即类型/原子切换) / 02 自动 HTTPS(分布式锁/1/3 续期/ask 闸) / 03 HTTP 栈与路由(显式顺序/预编译链) / 04 反向代理(12 策略/XFF 零信任) / 05 工程文化(xcaddy/caddytest)

### V8 深读·精简卷(5 篇,基线 c6a1f7c2)
01 Isolate 与堆(Handle/分代/Zone) / 02 解析器与字节码(惰性两层/反馈向量) / 03 Ignition 解释器(四层金字塔/CSA handler) / 04 对象模型与隐藏类(Map/ElementsKind/IC 三态) / 05 GC 与 Orinoco(单方向写屏障/并发标记)

### Prometheus 深读·卷一(7 篇,基线 b0f312b)
01 全景架构(装配/fanout/Appender 事务) / 02 Head 内存块(stripe 锁/XOR 编码/OOO) / 03 磁盘块与查询(TOC/16B 对齐 ID/三层剪枝) / 04 WAL 与压缩(段页格式/撕裂哨兵/原子发布) / 05 PromQL 引擎(两阶段评估/向量匹配) / 06 抓取与工程(SD 流水线/relabel 两阶段/stale NaN)

### Prometheus 深读·卷二(4 篇,基线 b0f312b)
07 remote 读写(WAL 即队列/hashmod 分片) / 08 rules 引擎(for 纯时间差/CopyState) / 09 K8s 服务发现(informer 快照重算) / 10 web 与 API(就绪三态/jsoniter 流式)

### V8 深读·卷二(5 篇,基线 c6a1f7c2)
06 TurboFan 与 Turboshaft(Sea of Nodes/turbolev 默认/deopt 翻译对偶) / 07 Torque 与 builtins(三层形态/labels/CEntry) / 08 快照与 code cache(embedded blob/三级容错梯度) / 09 测试与工程(natives 驱动/stress 变体/foozzie 差分)

### MinIO 深读·卷二(5 篇,基线 7aac2a2)
07 IAM 与服务账号(四段鉴权链/Deny 优先) / 08 SSE 加密(三形态/密钥层级/加密先于 EC) / 09 S3 API 层(SkipClean/PutObject 全链/multipart) / 10 分层与联邦(tiering/free-version/站点复制)

### Docker 深读·卷三(4 篇,基线 f6132db/579be22)
13 transfer 与 diff(单方法框架/whiteout) / 14 seccomp notify 与 console(pidfd_getfd/TIOCSCTTY) / 15 CDI 设备注入(七类编辑) / 16 测试与集成(failpoint/8 架构静态发布)

### PostgreSQL 深读·卷四(4 篇,基线 8c7a74c)
15 GIN 索引(entry tree/pending list 代价) / 16 BRIN 索引(revmap O(1)/摘要失真) / 17 WAL 归档与 PITR(.ready/.done 协议/backup_label 信任锚) / 18 并行查询(DSM shm_toc/chunk 原子分发)

### zstd 深读·扩展(2 篇,基线 d79e723)
07 多线程与字典(job 分片按序输出/cover+fastcover 训练/CDict 预填) / 08 流式 API 与 Seekable(三 endOp/hostageByte/seek table)

### QuickJS 深读·扩展(2 篇,基线 04be246)
07 JSON 与 REPL(复用词法器递归下降/reviver 三参/自宿主 REPL/启动 0.062ms) / 08 性能实测(与 node 2.85× 差距量化/启动 158KB/GC 触发条件)

### DuckDB 深读·卷一(7 篇,基线 7e886f44)
00 导读(三大纠偏:libpg_query 已移除/Vector 瘦身/单写者实为乐观) / 01 全景架构(启动八步/ATTACH 统一主库/扩展静态=动态特例) / 02 类型系统与向量化(string_t 16 字节/空指针 NULL 编码/sel 零拷贝) / 03 解析与绑定(自研 PEG+packrat/名字解析决策树/prepared 靠 catalog 身份校验) / 04 优化器(45 个固定 pass/DPhyp+贪心/统计传播顺带改写) / 05 执行器与并行(三规则切块/Sink-Combine-Finalize 三相/动态领批) / 06 事务与 MVCC(快照=区间/undo 记新值/先WAL后发布)

### Envoy 深读·卷一(7 篇,基线 v1.39.1/b579d07d)
00 导读(三大纠偏:数据面无锁/hot restart 是换二进制/admin 非独立线程) / 01 全景架构(main+N worker/ThreadLocal 广播当锁/启动顺序即依赖拓扑) / 02 配置与 xDS(四 delivery 一抽象/ACK 即排序信号/warming 握手扣 CDS ACK) / 03 网络与监听(reuse_port 分叉/三道 accept 闸/水位线背压/chain 定型) / 04 HTTP 与路由(codec 封闭协议差异/流是一等公民/预算制重试/shadow) / 05 集群与负载均衡(per-thread 视图/priority spill/panic 50%/host 复用/统一建连公式) / 06 热重启与工程(三通道接力/stats 重放/GuardDog 宁杀勿挂)

### NATS 深读·卷一(7 篇,基线 8f3f31b0)
00 导读(三大纠偏:每连接两 goroutine/集群无共识/网关乐观模式已废) / 01 全景架构(一 struct 七 kind/两 goroutine/顺路冲刷) / 02 协议与解析(76 状态状态机/零拷贝/慢消费者踢除) / 03 订阅匹配(trie+两级缓存/影子权重/分级失效) / 04 集群与网关(RS± 边沿触发/兴趣投影/账号聚合/无共识论证) / 05 JetStream(每流一组 Raft/WAL 即 filestore/ack 即日志) / 06 安全与工程(nkey 公钥即身份/decentralized JWT/零依赖纪律)

### systemd 深读·卷一(7 篇,基线 1f66b524)
00 导读(三大纠偏:11 种类型/PID1 不再 fork 用 executor/Requires≠After) / 01 全景架构(Manager 单例/启动九步/execve 自重启/虚表 11 类型) / 02 unit 与 job(atom 位掩码/合并真值表/事务十步/DFS 删环自愈) / 03 事件驱动底座(五时钟双堆/accuracy 合并唤醒/SIGCHLD 收尸接力) / 04 服务进程管理(executor 化/六 Type/单定时器多语义/KillMode 三级) / 05 cgroup 集成(unit 树=cgroup 树/七 mask 双向 realize/BPF 七件套) / 06 激活器与工程(fd=3 协议/timer 双轨/AGENTS.md AI 金丝雀)

### DuckDB 深读·卷二(6 篇,基线 7e886f44)
07 存储总览(双 header 原子提交/meta 链 64 分片/free list 自举/恢复三路对账) / 08 BufferManager(句柄状态机/tag 记账/无锁淘汰队列/溢出分格池) / 09 表存储(RowGroup 60 vector/写入不压缩 checkpoint 竞分/增量旁路/列级元数据复用) / 10 WAL 与 checkpoint(逻辑 WAL/先立字据/分叉并轨/一次指针比较恢复) / 11 压缩(analyze 竞分/Bitpacking 四模式/dict_fsst 三形态/ALP 替代 Chimp) / 12 Parquet(footer 即 I/O 计划/三层过滤下推/late materialization)

### systemd 深读·卷二(6 篇,基线 1f66b524)
07 journald(追加写 arena/三层交叉索引/可信字段边界/分组限速/seal) / 08 udev(worker 池/设备链串行化/token 编译规则/优先级链接栈/Varlink) / 09 sd-journal(值游标六键/Match 链跳转/k 路归并/链缓存二分) / 10 logind(scope 会话/pidfd 世代/inhibitor 分级/seat 抽象/无状态重启) / 11 networkd 与 resolved(声明式 .network/Link 状态机/四协议聚合/per-scope 缓存) / 12 工程文化(命名即协议/解析面 fuzz/自研测试框架/文档进 CI)

### Envoy 深读·卷二(6 篇,基线 v1.39.1/b579d07d)
07 QUIC(quiche 薄桥/CID 内核分流/同一 HCM/缺席接口硬保护) / 08 Wasm 与 Lua(proxy-wasm ABI/每 worker VM 克隆/协程挂起/沙箱边界) / 09 可观测性(三级 stat 查找/确定性采样/同步格式化异步落盘/CEL) / 10 TLS 与 SDS(BIO 桥接/ctx 级 SNI 选择/重建式热轮换/CRL 即刻生效) / 11 上游连接细节(SETTINGS 实测容量/CONNECT 三 pause 位/H1 专用化 H2 复用/hedging) / 12 管理面与生态(config_dump 组装/危险端点数据结构化/admin 过载豁免/生态盘点)

## 术语跨系统对照(精选)

### QuickJS 深读·扩展(2 篇,基线 04be246)
07 JSON 与 REPL(复用词法器递归下降/reviver 三参/自宿主 REPL/启动 0.062ms) / 08 性能实测(与 node 2.85× 差距量化/启动 158KB/GC 触发条件)

## 术语跨系统对照(精选)

| 术语 | 出现场景 |
|---|---|
| WAL/日志先行 | SQLite(04)、PG(03)、Kafka(04)、etcd(02)、LevelDB(08) |
| checkpoint | SQLite(04 回填)、PG(04 三步锚定)、etcd snapshot、Kafka HW |
| compaction/整理 | LevelDB(06-07)、Git repack(Git 04)、Kafka LogCleaner(04)、PG VACUUM(PG 07) |
| MVCC/可见性 | PG(06)、etcd revision、SQLite WAL 快照 |
| 表驱动 | FFmpeg URLProtocol(09)、Git refs 后端(Git 02)、Redis 命令表(Redis 02)、Nginx 模块(Nginx 07) |
| 唯一汇聚点 | FFmpeg retry_transfer(09)/schedule_update(16)、PG config 层级(PG 11) |
| 原子替换 | SQLite temp+rename、Git .lock/reftable(Git 02)、FFmpeg HLS .tmp(09) |
| 乐观并发+回退 | Git stat 缓存(Git 03)、PG get_format 剔除(卷一 02)/FFmpeg get_format(12)、K8s RV(07) |
| 字节预算/自律 | Git --window/--depth(Git 04)、PG ring buffer(PG 02)、zstd clevels(zstd 03) |

> AI 编码助手:GLM-5.3-Flash。
