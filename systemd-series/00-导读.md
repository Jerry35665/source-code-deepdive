# 《systemd 卷一》导读:如何使用本卷

> 本卷是《源码深读 · 系统开源项目解读系列》**第五系列**之十二。全部结论来自对 systemd(commit `1f66b524`,2026-09-14 镜像同步主干,版本 262~rc2)源码的逐行阅读,每处关键论断标注 `文件:行号`。

## 定位与三大纠偏

systemd 是 Linux 的 **PID 1:声明式依赖 + 事务制 job 引擎 + 单线程事件循环**的初始化与服务管理器。README 一句话:"System and Service Manager"(README:1)。

**三大纠偏**(与流行认知/二手资料相反):

1. **unit 类型是 11 种不是 12 种**(unit-def.h:9-24),且 UnitType 枚举竟定义在 basic 层(unit-def.h),不在 core;
2. **PID 1 已不再 fork 业务进程**——`exec_spawn` 序列化上下文后以 `CLONE_VM|CLONE_VFORK`(pidfd_spawn)拉起钉死的 `systemd-executor` 二进制,安全上下文全部在 executor 内应用(execute.c:525-530,exec-invoke.c:5172);传统"双 fork"仅残留在辅助路径;
3. **Requires= 不含 After= 也不含"跟死"语义**——依赖的行为差异全部来自 20 余种依赖名到 30 个正交 atom 的映射表(unit-dependency-atom.c:6-101);"After= 不会被 Requires= 隐含"的文档警告在代码里就是两张分离的 atom。

## 各章文件

| 文件 | 内容 |
|---|---|
| [01-全景架构](01-全景架构.md) | Manager/Unit/Job/Transaction 层次、启动九步、execve 自重启、11 类型虚表 |
| [02-unit与job](02-unit与job.md) | 两台正交自动机、依赖 atom 表、合并真值表、事务十步流水线 |
| [03-事件驱动底座](03-事件驱动底座.md) | sd-event 16 种事件源、双堆 accuracy、限速、SIGCHLD 收尸接力 |
| [04-服务进程管理](04-服务进程管理.md) | 六种 Type、executor 化、超时单定时器多语义、KillMode 三级 |
| [05-cgroup集成](05-cgroup集成.md) | unit 树=cgroup 树、控制器传播、OOM 事件链、七个 BPF prog |
| [06-激活器与工程](06-激活器与工程.md) | socket fd=3 协议、timer 双轨、path inotify、AGENTS.md AI 纪律 |

### 卷二(周边守护进程,基线同 1f66b524)

| 文件 | 内容 |
|---|---|
| [07-journald](07-journald.md) | 追加写 arena、三层交叉索引、可信字段边界、分组限速、seal |
| [08-udev](08-udev.md) | worker 池、设备链串行化、token 编译规则、优先级链接栈、Varlink |
| [09-sdjournal](09-sdjournal.md) | 值游标六键、Match 链跳转、k 路归并、链缓存二分、grep 无降级 |
| [10-logind](10-logind.md) | scope 会话、pidfd 世代、inhibitor 分级、seat 抽象、无状态重启 |
| [11-networkd-resolved](11-networkd-resolved.md) | 声明式 .network、Link 状态机、四协议聚合、per-scope 缓存 |
| [12-工程文化](12-工程文化.md) | 命名即协议、解析面 fuzz、自研测试框架、文档进 CI |

**卷二三大纠偏**:①udevd.c 只剩 91 行薄入口(主体在 udev-manager/udev-worker),控制面已换 Varlink;②bisect 表已被 ChainCacheItem 链缓存取代,bbox 记账交集已不存在;③session 追踪的 FIFO 已被 pidfd 取代(v258+,inhibitor FIFO 仍在)。

### 卷三(外围子系统,基线同 1f66b524)

| 文件 | 内容 |
|---|---|
| [13-nspawn](13-nspawn.md) | 三层进程、双层 mntns、API VFS 挂载表、ENOSYS seccomp、nftables DNAT、Varlink 注册 |
| [14-homed](14-homed.md) | 编排者+homework 子进程、五种存储、三副本 identity 对账、令牌派生口令 |
| [15-加密与TPM2](15-加密与TPM2.md) | 密钥级联、v258 PCR 7 退位、密封密钥=SRK 加密 RNG blob、策略分片、降级而非砖机 |
| [16-importd与portabled](16-importd与portabled.md) | 编排器+callout 两级、先哈希后解压、etag 跳过校验、分区级 image policy |
| [17-sysupdate](17-sysupdate.md) | 声明式 transfer、GPT label 即状态、varlink 通知 bootctl、vacuum 保底 |
| [18-creds凭据](18-creds凭据.md) | 两信任桶、AES256-GCM 三类密钥、服务只见明文、noswap+原子换入 |

**卷三三大纠偏**:①nspawn 的 machined 注册已 Varlink 优先(A3 报告);②TPM2 解锁默认绑 PCR 7 已是旧闻——v258 起注册端不再默认绑(C3 报告);③内核命令行上不存在 systemd.credentials=,真实选项是 systemd.set_credential=(F3 报告)。

### 卷四(可信启动链,基线同 1f66b524)

| 文件 | 内容 |
|---|---|
| [19-bootctl](19-bootctl.md) | src/bootctl 版图、ESP 探测、update 永不死局双保险、varlink 服务面、零参数广播 |
| [20-ukify与stub](20-ukify与stub.md) | PE 手工加段、外调签名测量、PCR 11/12/13 布局、.efifw 测量不对称缺口 |
| [21-measure与pcrlock](21-measure与pcrlock.md) | 虚拟 extend、策略摘要签名、pcrlock NV index 间接层、组件库即时间轴 |
| [22-sysext与confext](22-sysext与confext.md) | 一码两身份、私有 ns 三段式 merge、origin 指纹短路、/usr 禁入 |
| [23-kernel-install](23-kernel-install.md) | C 编排器+外部插件协议、staging 两段落盘、ENTRY-TOKEN 与 bootctl 同源 |
| [24-analyze](24-analyze.md) | 81 项加权 exposure、verify 跑真 manager、启动分析 D-Bus 时间戳单源 |

**卷四三大纠偏**:①bootctl 本体在 src/bootctl/,src/boot/ 是 EFI 侧源码(A4/B4 报告);②ukify 标 .efifw 不测量而 stub 会量入 PCR 11——本 commit 真实一致性缺口(B4 报告);③measure 签的是 PolicyPCR 摘要而非裸 PCR 值,签名摘要恒 SHA256(C4 报告)。

## 三种用法

1. **通读**:01→06,骨架→执行计划→事件底座→进程生命周期→资源控制→激活器;
2. **答疑**:为什么 systemd 单线程→03;一个 start 请求内部经历什么→02;守护进程怎么该写 READY=1→04;进程属于哪个 unit 谁说了算→05;LISTEN_FDS 从 3 开始为什么→06;
3. **自研借鉴**:02 章"事务+合并真值表"与 03 章"accuracy 双堆合并唤醒"是两个可直接移植的设计样本。

> AI 编码助手:GLM-5.3-Flash。
